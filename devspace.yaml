version: v2beta1
name: sample-golang

images:
  # By default ./Dockerfile will be used
  app:
    image: registry.local.ertia.cloud/${IMAGE}
    tags:
      - ${IMAGE_TAG}
    buildKit:
      inCluster: {}

vars:
  IMAGE: sample-golang
  IMAGE_TAG: latest
